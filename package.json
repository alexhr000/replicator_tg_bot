{
  "name": "telegram-bot-worker",
  "version": "1.0.0",
  "description": "Telegram bot for Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:bot-token": "wrangler secret put BOT_TOKEN",
    "secret:admin-ids": "wrangler secret put ADMIN_IDS"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
